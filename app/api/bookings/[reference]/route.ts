import {NextResponse} from "next/server";import {prisma} from "@/lib/db";
export async function GET(req:Request,{params}:{params:Promise<{reference:string}>}){const {reference}=await params;const email=new URL(req.url).searchParams.get("email")?.toLowerCase();if(!email)return NextResponse.json({message:"Email required"},{status:400});const b=await prisma.booking.findUnique({where:{reference},include:{vehicle:true}});if(!b||b.guestEmail?.toLowerCase()!==email)return NextResponse.json({message:"Booking not found"},{status:404});return NextResponse.json({reference:b.reference,vehicle:`${b.vehicle.year} ${b.vehicle.make} ${b.vehicle.model}`,startAt:b.startAt,endAt:b.endAt,status:b.status,paymentStatus:b.paymentStatus,totalCents:b.totalCents,amountPaidCents:b.amountPaidCents,amountDueCents:b.amountDueCents})}
