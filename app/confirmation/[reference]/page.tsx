import Link from "next/link";import {notFound} from "next/navigation";import {SiteNav} from "@/components/SiteNav";import {prisma} from "@/lib/db";import {formatMoney} from "@/lib/money";
function rentalTime(d:Date,timeZone:string){return new Intl.DateTimeFormat("en-US",{timeZone,weekday:"long",month:"long",day:"numeric",year:"numeric",hour:"numeric",minute:"2-digit",timeZoneName:"short"}).format(d)}
export const dynamic="force-dynamic";export default async function Confirmation({params}:{params:Promise<{reference:string}>}){const {reference}=await params;const b=await prisma.booking.findUnique({where:{reference},include:{vehicle:true,rentalPackage:true,pickupLocation:true}});if(!b)notFound();return <><SiteNav/><main className="container section"><div className="loginBox" style={{margin:"40px auto",width:"min(680px,100%)"}}><div className="eyebrow">Reservation received</div><h1>{b.reference}</h1><p className="muted">Your booking has been created. If Stripe is configured, final confirmation occurs after the verified payment webhook.</p><div className="summary"><div className="row"><span>Vehicle</span><b>{b.vehicle.year} {b.vehicle.make} {b.vehicle.model}</b></div><div className="row"><span>Pickup</span><b>{rentalTime(b.startAt,b.pickupLocation.timezone)}</b></div><div className="row"><span>Return</span><b>{rentalTime(b.endAt,b.pickupLocation.timezone)}</b></div><div className="row"><span>Status</span><b>{b.status}</b></div><div className="row"><span>Payment</span><b>{b.paymentStatus}</b></div><div className="row total"><span>Total</span><span>{formatMoney(b.totalCents)}</span></div></div><Link href="/cars" className="btn primary" style={{marginTop:18}}>Back to Fleet</Link></div></main></>}
